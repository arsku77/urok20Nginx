RewriteEngine on
# If a directory or a file exits, use the request directrly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# Otherwise foward the request to index.php
RewriteRule . index.php
